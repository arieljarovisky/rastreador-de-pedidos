{
  "name": "rastreador-de-pedidos",
  "private": true,
  "description": "Posta / LupoEnvios — rastreador de pedidos",
  "scripts": {
    "predev": "npx --yes kill-port 4000 5173 5174",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:setup": "npm run db:setup --prefix backend",
    "db:reset": "npm run db:reset --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
